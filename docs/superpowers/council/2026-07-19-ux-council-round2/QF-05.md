# QF-05 [P1/M] Jargon + voice copy pass (DNF, clk, rules, recovery, attribution)

## Files
- src/components/LeaderboardList.tsx:91
- src/modes/Boards.tsx:644
- src/App.tsx:1300 (End dialog copy)
- src/domain/formatting.ts:14-16
- src/components/TeachingGate.tsx + popup copy
- src/modes/AppShell.tsx (footer)
- src/race/PreRacePreview.tsx
- src/race/RaceRecoveryInterstitial.tsx:30-43
- src/modes/challenges/Browse.tsx:286
- src/race/RaceResults.tsx:444-470
- src/modes/You.tsx:28-41
- src/modes/Home.tsx:291-304

## Changes
Spell out 'DNF — did not finish' in the two bare <h3>DNF</h3> headers and the End-Run dialog string (the expansion already exists in RaceResults). Change formatTimeAndClicks to output 'clicks' instead of 'clk' — one shared formatter, propagates everywhere. Add one flavor-legend line to How-to-play ('Famous picks early week, Weird Thu-Fri, Hard weekends — the badge tells you which'). Add a permanent 'How to play' link in the footer reusing TeachingGatePopup, and one rules line on PreRacePreview ('Click links inside the article — no search, no back button'). Fix recovery copy: 'End Old Run'→'End Run', 'Retry Resume'→'Resume run'. Give system dailies their own attribution ('Today's daily') instead of 'Created by VWiki Race'. Warm the solo-DNF board state ('Nobody's finished this one yet — be the first'). Make the claim CTA streak-aware ('You're 5 days in — that streak only lives in this browser') and split You's claim copy for never-played visitors ('Race today's daily to start your stats').

## Acceptance
- Zero unexpanded 'DNF' or 'clk' reachable before a player's first race
- Rules are re-accessible forever via footer; Preview restates links-only/no-back before the clock can start
- Recovery screen uses the app's one End Run vocabulary; no 'Created by VWiki Race' rows in Browse
- A single-DNF board on Home carries an invitation line, not a bare failure row

## Risk
Low — pure copy plus one formatter; budget for copy-locked test updates.

## Judge A (amend)
1. DROP (or properly rescope) the "End Old Run"→"End Run" / "Retry Resume"→"Resume run" recovery-copy line item as currently written. Verified: PKG-11, shipped in this very round (App.tsx:1453-1461 code comment), deliberately ratified "End Run" vs "End Old Run" as two members of one Title-Case naming family specifically so the recovery variant reads as distinct from a live run's End Run — and the confirm dialog already ships "Confirm End Old Run" for the recoveryRun case (App.tsx:1460). QF-05 cites only RaceRecoveryInterstitial.tsx:30-43 (the trigger button), not App.tsx:1460 (the confirm dialog). Renaming just the trigger to "End Run" while leaving "Confirm End Old Run" in place creates a worse, *new* label mismatch inside a single flow (click "End Run" → dialog says "Confirm End Old Run") — this is a correctness regression, not a copy improvement, and it re-litigates a same-round decision without new evidence, which is exactly what the owner said he doesn't want. If the owner does want "End Old Run" gone, that's a real decision to bring back to him explicitly, not a line item to fold silently into a copy pass. "Retry Resume"→"Resume run" is fine to keep in isolation (no established competing term, genuinely reads clearer against the h2 "Resume your previous run").
2. RESCOPE "make the claim CTA streak-aware" out of this package into its own properly-estimated ticket. Verified: RaceResults.tsx's ClaimCta has no access to accountStats/dailyStreak today (no such prop in its signature) — this needs new plumbing (App.tsx → RaceFlow → RaceResults → ClaimCta), not "pure copy." Worse, RaceResults.tsx:91-98 documents the exact reason a *different* stats-derived value (preRaceCompletions) is deliberately snapshotted rather than read live: "M2 fix... the live/current accountStats... can still read stale (pre-refetch) or already-advanced by the time this screen renders, in either direction." Wiring a live streak count into ClaimCta reopens that exact class of bug unless it's snapshotted the same way — real design/engineering work, not a copy tweak. The package's own risk line ("Low — pure copy plus one formatter") is inaccurate for this item; either drop it from this weekend's batch or replace it with a non-live-data version (e.g. a generic "that streak only lives in this browser" line with no day-count) that carries the acknowledged low risk.
Everything else in the package should ship as-is this weekend.

### Judge A reasoning
Verified every cited file:line and the two most load-bearing claims against the round-2 screenshots; the package is well-researched and mostly correct, but two of its nine changes are mis-scoped in ways that matter to this owner specifically.

Confirmed accurate, worth doing now, low-risk as claimed:
- Bare `<h3>DNF</h3>` at LeaderboardList.tsx:91 and Boards.tsx:644, and bare "DNF" in the End-Run dialog copy (App.tsx:1300: "It'll count as a DNF with 1 click.", visible in flow-05-results.png) — RaceResults.tsx:411 already has the "DNF — Did not finish" expansion, confirming a real, cheap-to-close inconsistency reachable during a player's very first race.
- `formatTimeAndClicks` (domain/formatting.ts:14-16) is genuinely the one shared formatter — grep shows every "clk" instance in the app (Home, Boards, LeaderboardList, BoardSnippet, RaceResults, RaceMode, ChallengeDetail, challengeCard) routes through it, confirmed live in mobile-03-boards-today.png ("0:18 · 1 clk"). One-line change, propagates everywhere exactly as claimed.
- Footer (AppShell.tsx:314-333) has no "how to play" link — confirmed by direct read, and shouldShowTeachingGate (domain/teachingGate.ts) permanently hides TeachingGate once `stats.totals.completed > 0`, so the rules genuinely vanish forever with no other affordance. Real IA gap, not invented.
- PreRacePreview.tsx has no rules restatement (confirmed via file read and mobile-09-preview.png) — the "links only, no back button" line only ever appears inside TeachingGatePopup, which itself disappears after first completion.
- Browse.tsx:286 "Created by {displayName}" applies uniformly, including to system dailies — confirmed live in mobile-04-challenges.png ("Created by VWiki Race" on Challenge #4/#7/#8, all DAILY-badged).
- Solo-DNF cold board state is not hypothetical — mobile-03-boards-today.png is literally "No completed runs yet." directly above a single DNF row, matching the "cold front door" complaint exactly.
- You.tsx's claim CTA for a never-played guest ("Playing as a guest. Claim your name so your stats stay yours.") sits directly above a Stats block that is 100% "No data yet." (mobile-07-you.png) — a real, visible inconsistency implying something exists to protect when nothing does.
- The proposed flavor-legend line ("Famous picks early week, Weird Thu-Fri, Hard weekends") is factually correct against dailyEditorial.ts's actual schedule (dailyFlavorForCentralDate: Sat/Sun→hard, Thu/Fri→weird, else→recognizable) — good sign of care in the package's authorship.

Flagged as needing amendment (detailed in amendments): the recovery-copy rename conflicts with a decision (PKG-11) ratified in this same round with explicit stated reasoning, and as scoped would introduce a new same-flow label mismatch rather than fix one; and the "streak-aware claim CTA" is billed as pure copy but actually requires new data plumbing into a component whose file already carries a documented scar (M2 fix) from exactly this class of live-stats-staleness bug. Both are the kind of thing this owner explicitly said he doesn't want (re-litigated decisions, scope creep hiding inside what's billed as a small safe change) — everything else in QF-05 is real, cheap, and should ship this weekend.

## Judge B (amend)
1. Drop the formatTimeAndClicks "clk"→"clicks" change from this package entirely, OR carve it out as its own separate ticket requiring an explicit owner ruling to amend spec Global Invariants #1 and #2 (both literally quote "clk"); do not ship it as an in-package copy fix. If dropped, tighten the acceptance criterion "Zero unexpanded DNF or clk reachable before a player's first race" to drop the "clk" clause (or replace with a first-visit-only in-context expansion, e.g. in the teaching-gate popup text, that doesn't touch the shared formatter/global invariant strings).

2. Add `src/components/BoardSnippet.tsx` and `src/domain/boardSnippet.ts` to the files list, and specify the actual logic change needed: distinguish "0 rows total" (genuinely empty) from "N DNF rows, 0 placements" (solo/all-DNF) in `boardSnippetRowsFromBoard`/`BoardSnippet`, and only then attach the invitation copy. If that logic change is judged too large for this cycle, split it out as its own BACKLOG item rather than bundling it into a "Low risk" copy pass.

3. Either drop the You.tsx "never-played visitor" copy split from this package (defer it alongside the rest of the PKG-11-descoped stats-loading-state plumbing, as its own BACKLOG ticket), or scope it down to something achievable without new cross-file state — e.g. reuse the existing `hasIdentifiedSession` boolean the same way teachingGate.ts does, accepting that it will only distinguish "no session at all" from "has a session" (not truly "never raced"), and say so explicitly in the acceptance criteria so it isn't verified against a bar the code can't yet meet.

4. Scope "streak-aware claim CTA" as its own line item with the needed prop-threading named explicitly (App.tsx accountStats → RaceResults.tsx → ClaimCta), and require it read the SAME pre-race snapshot `showFirstFinishRitual`/`preRaceCompletions` already uses (not a post-race refetch) to avoid the timing bug class this file was already patched for once (the "M2 fix").

5. Fix the flavor-legend line's copy to match the real badge label: replace "Famous" with "Recognizable" (or whatever the owner decides the on-screen badge should say — but it must match `dailyFlavorLabel`'s actual output, not a synonym).

6. Note that reusing the how-to-play popup in the footer requires exporting `TeachingGatePopup` from TeachingGate.tsx (or refactoring `TeachingGate` to accept a controlled-open prop) — trivial, but should be named so it isn't missed as "just add a link."

With 1-4 resolved, re-scope this as two packages: a genuine QUICKFIX (DNF header/dialog expansion, recovery-copy fix, footer how-to-play link + export, PreRacePreview rules line, system-daily attribution via `createdBy.accountId === 'vwiki-race:daily'`, flavor-legend line with corrected wording) that ships this weekend as originally billed, and a separate BACKLOG/M-or-L item for the solo-DNF board logic, the never-played You copy, and the streak-aware claim CTA, all of which need real code changes and in two cases re-open work a previous cycle explicitly ruled out of scope.

### Judge B reasoning
Read all 12 cited files/lines plus the spec and round-1 README. Most of the package is real, correctly-scoped, low-risk copy work and should ship (DNF header expansion in LeaderboardList.tsx:91 and Boards.tsx:644 — confirmed bare `<h3>DNF</h3>`; End dialog bare-DNF string at App.tsx:1299-1301/1450; recovery button labels at RaceRecoveryInterstitial.tsx:33/42 — confirmed literal "Retry Resume"/"End Old Run"; PreRacePreview.tsx has no rules line today — confirmed; footer has no how-to-play link today — confirmed). But four items are misscoped hard enough to need amendment before this ships as a QUICKFIX:

1. SPEC-INVARIANT VIOLATION (blocking): changing `formatTimeAndClicks` (domain/formatting.ts:14-16) from "clk" to "clicks" contradicts the ratified spec's own literal text. Global Invariant #1 in docs/superpowers/specs/2026-07-18-ux-redesign-modes-design.md:44 reads '`0:38 · 5 clk`' verbatim, and Invariant 2 (line 188) reads '`✓ 0:42·6clk`' verbatim — this abbreviation is quoted as the *contract*, not incidental copy, and is cross-referenced as such in doc-comments in formatting.ts, types.ts, challengeCard.ts, StateChip.tsx, LeaderboardList.tsx, RaceMode.tsx, Boards.tsx. Shipping this under a "pure copy, Low risk" QUICKFIX would silently overturn two numbered global invariants — exactly the failure mode the round-1 README names as systemic cause #3 ("mockups/spec treated as inspiration, not contract"). Blast radius is also underestimated: "clk" appears in 48 assertions across 6 test files (App.test.tsx alone: 38), not "one shared formatter, propagates everywhere" as a small edit. This item needs either an explicit owner ruling to amend the spec invariant text itself, or must be dropped from this package.

2. Files list is missing the file the package's own acceptance criterion depends on. "A single-DNF board on Home carries an invitation line" targets Home's board, which renders via `src/components/BoardSnippet.tsx` + `src/domain/boardSnippet.ts` (boardSnippetRowsFromBoard) — neither is in the package's file list. The two DNF-header files actually cited (LeaderboardList.tsx:91 = Challenge Detail's board, Boards.tsx:644 = Stats tab's board) are different renderers that never touch Home. Worse: `BoardSnippet`'s empty-state branch only fires at `rows.length === 0`, and DNF rows are merged into that same array — a lone DNF (1 row, 0 placements) already skips the empty-state message and renders as an ordinary list row with no hook for an "invitation line." This needs new conditional logic in files this package doesn't list, not a copy swap.

3. You.tsx's "never-played" claim-copy split re-opens a gap PKG-11 explicitly descoped last cycle. You.tsx:62-74's own comment (written for PKG-11's remainder fix) states that distinguishing "loading" / "errored" / "guest, nothing to fetch" would need new state threaded through App.tsx → AppShell.tsx → You.tsx, since `accountStatsProjection` carries no such signal, and was deliberately "descoped to its own ticket." QF-05 asks for exactly that distinction ("never-played" vs "played, not claimed") under a "pure copy... Low risk" banner — it isn't pure copy, it's the same plumbing PKG-11's own judges already ruled out of scope.

4. "Streak-aware claim CTA" needs a new prop, not just new copy, and carries a race-condition risk class this exact file already had to fix once. RaceResults.tsx's `ClaimCta` (line 444) only receives `displayName`/`onClaimIdentity`; no streak data reaches Results today. Threading `dailyStreak` in means a new prop chain (App.tsx → RaceResults.tsx → ClaimCta), and the file's own `preRaceCompletions`/`showFirstFinishRitual` machinery exists specifically because pre-race-snapshot-vs-post-race-refetch timing is a documented ("M2 fix") source of bugs on this screen — showing a streak number that may not have incremented yet risks the same class of bug, unaddressed by the package.

Minor (non-blocking, fix in the same pass): the package's own suggested flavor-legend copy says "Famous picks early week" but `dailyFlavorLabel` (domain/dailyEditorial.ts:84-86) renders the actual badge as "Recognizable," never "Famous" — ships a legend that describes a badge word the UI doesn't show. Also "reusing TeachingGatePopup" in the footer requires exporting it first — it's currently a private, non-exported function inside TeachingGate.tsx.

## OWNER-PROXY RULING (binding)
RULING: ship MINUS two items. (1) DROP the recovery-copy renames ('End Old Run'->'End Run' etc.) — Judge A verified PKG-11 deliberately ratified that naming family this same round; re-litigation. (2) DROP the formatTimeAndClicks 'clk'->'clicks' change — Judge B verified the spec's global invariants literally quote 'clk'; parked as an owner question instead. KEEP: DNF spelled out in the two bare headers + End-Run dialog, the flavor-legend line in How-to-play, the permanent footer 'How to play' link reusing TeachingGatePopup, and the one-line rules note on PreRacePreview.
