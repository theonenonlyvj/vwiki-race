# QF-07 [P1/M] Truncation, alignment & input hardening

## Files
- src/styles.css:593-608, 1794-1801 (+ font-weight literals)
- src/App.tsx (continueAsGuest ~921, createVGamesAccount ~964)
- src/modes/challenges/Browse.tsx:169-188
- src/modes/Home.tsx:120-154, 172-181
- src/hooks/useDailyCountdown.ts / .run-metrics styles

## Changes
Fix .path-history so per-span ellipsis actually fires (children get min-width:0 / flex:0 1 auto; container stops pre-clipping) — verify 'Near side of the Moon' renders a real … at 320/390px. Give the shared .board-snippet li name span min-width:0 + ellipsis (same 'Bug A fix' pattern its siblings already have) and justify-self:end on the time column so times right-align. Add the loginRequestLock-style synchronous ref to continueAsGuest, createVGamesAccount, and Browse's submitChallenge. Add font-variant-numeric: tabular-nums to the Home countdown line and .run-metrics. Batch-replace all font-weight 750/800/850/900 with 700 (Fredoka's real ceiling — pixel-identical by construction, diff screenshots to prove it). Call errorReporter.report from Home's swallowed board-fetch catches so outages stop masquerading as 'No completed runs yet.' Thread a catalogLoaded flag to Home so an empty-but-loaded catalog says 'No challenges available right now' instead of 'Loading challenge catalog' forever.

## Acceptance
- Long titles show real ellipses in the HUD strip and in board rows at 320/390px; no mid-word hard clips anywhere
- Times form a clean right-aligned column in board snippets
- Double-tap on guest/create/create-challenge fires exactly one request
- Countdown and HUD timer digits no longer shift width tick-to-tick
- No font-weight above 700 remains; before/after screenshots pixel-identical
- Board fetch failures appear in error telemetry; empty catalog gets honest copy

## Risk
Low — mechanical CSS/ref changes copying patterns that already exist elsewhere in the same files.

## Judge A (amend)
1) Sequence the .run-metrics tabular-nums change AFTER the in-flight HUD-timer-ghost hotfix, don't bundle it now. Verified: .race-hud .run-metrics (styles.css ~1225) is the exact 'metrics' grid-area rendered inside .race-hud (src/race/RaceMode.tsx:100, class='run-metrics') — this is the same element the task brief flags as 'renders as a low-contrast ghost overlapping the path strip (hotfix in flight)', visibly confirmed in flow-03-race-screen-hudzoom.png / flow-04b-after-settle-hudzoom.png (a faint ghosted line sits behind 'Moon -> Near side of th'). Landing a CSS edit to the same block a separate hotfix is actively touching risks merge collision and makes the tabular-nums diff unverifiable while the element is mid-repair. Pull this one line out of QF-07 and land it after the ghost-timer fix ships (or fold it into that fix's own diff).

2) Drop or re-verify the 'time column right-align (justify-self:end)' sub-claim before spending effort on it. Read the actual markup (BoardSnippet.tsx / LeaderboardList.tsx → .board-snippet li { grid-template-columns: 2.4em 1fr auto }): the time span is the grid's last, auto-sized track, and the 1fr name column absorbs all remaining space up to the container's right edge by definition of CSS Grid fr-track math — so each row's time text is already flush against the row's right edge today, with no independent slack for justify-self:end to close. None of the r2 screenshots contain two board-snippet rows with differently-lengthed times to demonstrate an actual visible misalignment (every populated board currently has 0-1 real rows, e.g. mobile-03-boards-today.png, flow-05-results.png). This may be a phantom finding born from sparse test data rather than a real defect — verify with a synthetic multi-row fixture before claiming it as a fix; don't list 'times form a clean right-aligned column' as an acceptance criterion if the underlying code shows it's already true.

3) Correct the risk label — 'Low, mechanical CSS/ref changes copying patterns that already exist' is accurate for 5 of the 7 bundled changes (path-history ellipsis, board-snippet name ellipsis, the three double-submit ref locks, tabular-nums, font-weight cleanup) but NOT for two: errorReporter.report from Home's catches, and the catalogLoaded flag. Verified: errorReporter is only ever instantiated in src/main.tsx and passed solely to <ErrorBoundary reporter={...}> — it is not threaded into App.tsx or any modes/ component today (grep for errorReporter/ErrorReporter across src/App.tsx and src/modes turns up nothing), so wiring it into Home's board-fetch catches requires adding a new prop through App→Home, not copying an existing pattern. Likewise there is no loading-vs-empty distinction anywhere today (challenges state initializes to [] with no companion boolean), so catalogLoaded is new state, not a copy. Both are still worth doing and still low-risk in absolute terms (Home is only exercised via full-App-mount tests, so a new optional prop is cheap to add without breaking existing tests), but the brief should say so accurately rather than imply it's pure boilerplate-copying — an owner skimming 'Low risk: mechanical' shouldn't be surprised to find two new prop-signature changes inside.

### Judge A reasoning
Verified the package's evidence directly against code and the cited screenshots rather than taking the brief at face value, and the core substance holds up well.

Confirmed real and severe: the HUD breadcrumb clip is not cosmetic exaggeration — flow-03-race-screen-hudzoom.png shows 'Moon -> Near side of th' hard-clipped mid-word with zero ellipsis, on the one screen (the live race) that is the entire point of the daily ritual. Code inspection explains exactly why: .path-history span uses flex: 0 0 auto (flex-shrink:0), so when total breadcrumb width exceeds the HUD's available space, the container's own overflow:hidden clips wherever the flex line happens to end rather than letting any single span's own text-overflow:ellipsis engage. The proposed fix (flex:0 1 auto + min-width:0) is the textbook correction and is genuinely S/low-risk CSS.

Confirmed real: login() already carries a loginRequestLock synchronous ref (App.tsx:236/1012/1024/1038) that continueAsGuest and createVGamesAccount conspicuously lack — both gate only on React state (authBusy), which has a real double-fire window before re-render. Browse's submitChallenge has the same gap (isCreating is set but never checked at entry). Copying the existing ref-lock pattern into the two missing spots is exactly the kind of small, real hardening a P1 quickfix should do.

Confirmed real: the base .board-snippet li (used by BoardSnippet.tsx and LeaderboardList.tsx — Results, Home's board cards, Challenge Detail's leaderboard) has no ellipsis/min-width:0 on its name span, while the sibling .board-trend-unranked/.board-roster variants explicitly do (styles.css's own 'Bug A fix' comment documents this exact asymmetry). A long guest display name (uncapped client-side) would genuinely overflow there today.

Confirmed real: only Fredoka 400/500/600/700 are imported (main.tsx) and font-synthesis:none is set globally (:root, styles.css:25), so every font-weight: 750/800/850/900 literal in the stylesheet is already rendering at 700 today with no synthetic bolding — batch-replacing the literal to '700' is provably pixel-identical, and is good hygiene (removes ~35 misleading magic numbers) at genuinely zero visual risk.

Confirmed real: Home's board-fetch catches swallow every error into the same 'no completed runs yet' rendering (no errorReporter call anywhere), and hero/challenges have no loaded-vs-empty distinction, so an empty catalog is indistinguishable from 'still loading' forever. Both are honest gaps worth closing now rather than discovering via a support message.

This is not scope creep and doesn't re-litigate anything shipped in round 1 (teal CTAs, chrome-free takeover, casual framing are untouched) — it's exactly the 'find what's next' hardening pass the owner asked for, and it's taste-positive: an unellipsised mid-word clip on the one screen that matters most is the kind of thing that reads as sloppy on a product built around daily-ritual confidence. With the three corrections above (pull the .run-metrics change out of the collision path with the in-flight hotfix, verify-or-drop the phantom time-alignment claim, and be honest that two of the seven bundled changes are new plumbing rather than copies), this is a strong, shippable-this-weekend package.

## Judge B (amend)
1. FILES LIST IS INCOMPLETE for two of the four changes — both require new prop-plumbing the package doesn't scope:

   (a) `errorReporter.report` from Home's board-fetch catches: verified `errorReporter` (src/main.tsx:17) is instantiated ONCE in main.tsx and passed only to `<ErrorBoundary reporter={errorReporter}>` (main.tsx:28) — it is not exported as a singleton, not in React context, and Home.tsx's props (src/modes/Home.tsx:54-96) have no reporter field. There is zero existing precedent in this codebase for a mode component calling `errorReporter.report` — grepped every .tsx/.ts file, the only `.report(` call site is ErrorBoundary.tsx:21. This is not "mechanical... copying patterns that already exist elsewhere," it's new architecture: either thread the single main.tsx instance through `main.tsx → App.tsx → AppShell.tsx → Home.tsx` (3 more files, none listed) or instantiate a second `createErrorReporter({apiOrigin})` inside App.tsx. If the builder takes the shortcut of a second instance, note the subtle bug: `reportedKeys`/`reportCount` (src/services/errorReporting.ts:39-40) live in the closure, so a second instance silently doubles the intended MAX_REPORTS_PER_PAGE_LOAD=10 ceiling and loses cross-source dedup with ErrorBoundary's crashes. Add main.tsx + AppShell.tsx to the files list and specify "reuse the single main.tsx instance," not a fresh one.

   (b) `catalogLoaded` flag to fix eternal "Loading challenge catalog": confirmed real — `selectHomeHeroChallenge` (src/domain/challengeSelection.ts:78-91) returns `null` whenever `challenges` is empty, which Home.tsx:172-181 renders as "Loading challenge catalog" unconditionally, with no way to distinguish still-loading from loaded-but-empty/failed. App.tsx already tracks this exact case with `catalogLoadFailed` (App.tsx:183, set at line 474) but that state is never passed to AppShell or Home today (confirmed via grep — zero downstream consumers). AppShell.tsx:225-242's `<Home>` call site has no loaded/failed prop. Add App.tsx (the `catalogLoadFailed`/challenges-loading block, ~403-494) and AppShell.tsx to the files list — as written, a builder following only "Home.tsx:120-154, 172-181" has no flag to thread.

2. NO TEST REQUIREMENTS NAMED, despite an established precedent to mirror. App.test.tsx:4647 already has "disables the button and fires only one request while a random-challenge creation is in flight (no double-fire)" covering `randomChallengeLockRef`. The package's double-submit fixes for `continueAsGuest`, `createVGamesAccount`, and Browse's `submitChallenge` should each get an equivalent test at that same location/pattern — name it explicitly rather than leaving "double-tap fires exactly one request" as a visual-only acceptance criterion. Similarly, the font-weight batch-replace claims "diff screenshots to prove pixel-identical" but this repo has no screenshot/visual-regression tooling (verified: package.json scripts are vitest-only, no Playwright/Percy) — that verification is the manual council-shots process, not a CI gate; say so explicitly so it isn't dropped as "someone else's job."

3. NOT REDUNDANT, VERIFIED DISTINCT: I checked whether Browse's `submitChallenge` (Browse.tsx:169-188) is already covered by App.tsx's `challengeLockRef` (used in `createChallenge`/`createChallengeWithSession`, App.tsx:705-724) — it is not. `challengeLockRef.current` is set every render from `challengeIsLocked` (App.tsx:273-278), a *race-controller-phase* lock ("is a run currently active"), not a synchronous same-tick double-click guard. The `submitChallenge`/`continueAsGuest`/`createVGamesAccount` gap is real and distinct from that existing lock — good diagnosis, proceed as designed for these three.

4. SEQUENCING RISK with the in-flight HUD-timer-contrast hotfix (not this package's fault, but worth flagging before merge): both the hotfix and this package's `.path-history`/RaceMode.tsx path-strip work (styles.css:576-628) and the `.run-metrics` tabular-nums addition (styles.css:1225 area, same `.race-hud .run-metrics` block the ghost-overlay bug lives in) touch the same HUD region. Land the contrast hotfix first, or rebase QF-07 after it, to avoid a silent stomp on the z-index/opacity fix.

5. TECHNICAL SPOT-CHECK (confirmed correct, no change needed): the fictional 750/800/850/900 weights genuinely resolve to 700 today per the CSS font-matching algorithm, since @fontsource/fredoka only ships 300/400/500/600/700.css (verified in node_modules) and font-weight fallback search for any value ≥500 looks upward first then downward, landing on 700 in every one of these cases — so "pixel-identical by construction" is an accurate, not just optimistic, claim. The `.path-history`/`.board-snippet li` diagnoses and prescribed fixes (flex:0 1 auto + min-width:0 on path spans; minmax(0,1fr) + ellipsis on the board-snippet name span, mirroring the already-shipped PKG-10 pattern at styles.css:1052-1058) are technically sound and correctly cite un-touched-by-round-1 selectors (the base `.board-snippet li` at 1794-1801 has no prior PKG comment, unlike its `.board-trend-unranked`/`.board-roster`/`.trend-row-toggle` siblings) — proceed as designed.

Net: proceed as designed on the CSS-only truncation/alignment fixes and the three double-submit locks (well-diagnosed, correctly scoped, genuinely low risk). Add App.tsx/AppShell.tsx/main.tsx to the files list for the errorReporter and catalogLoaded pieces before a builder starts, specify reusing the single errorReporter instance, and name the three new double-submit tests plus a note that the screenshot-diff verification is manual.

### Judge B reasoning
Verified every load-bearing claim against the actual source. Confirmed accurate and low-risk: (1) `.path-history span` (styles.css:593-608) truncation gap — traced why it clips mid-word instead of ellipsizing (flex:0 0 auto items with max-width DO self-truncate correctly in isolation, but the container's overflow:hidden clips a partially-visible last span at an arbitrary point before its own ellipsis renders — the prescribed flex:0 1 auto + min-width:0 fix is the standard, correct pattern for this exact failure mode). (2) `.board-snippet li` (styles.css:1794-1801) has grid-template-columns:2.4em 1fr auto with zero overflow handling on the name span — confirmed by reading BoardSnippet.tsx:49-52 and LeaderboardList.tsx:59-63, both render the raw name in a bare `<span>`; this selector, unlike its `.board-trend-unranked`/`.board-roster`/`.trend-row-toggle` siblings, was never touched by the already-shipped PKG-03/PKG-10 fixes (no PKG comment attached), so this is genuinely unfixed, not round-1 rework. (3) `continueAsGuest`/`createVGamesAccount` (App.tsx:921-1009) use only `setAuthBusy(true)`, an async state setter, with no synchronous guard — confirmed by contrast with `login()` (App.tsx:1011-1041), which already uses the `loginRequestLock` ref the package wants copied elsewhere; and confirmed Browse's `submitChallenge` (Browse.tsx:169-188, uses only `setIsCreating`) is NOT already covered by App.tsx's `challengeLockRef`, which I traced to be a race-phase lock (App.tsx:273-278), not a double-click guard — so this is a real, distinct gap, not redundant scope. (4) The fictional font-weight claim checks out exactly: @fontsource/fredoka ships only 300-700, and CSS's font-weight fallback algorithm resolves every value the codebase uses above 700 (750/800/850/900) to 700, so the "pixel-identical by construction" claim is provably true, not hopeful.

Found two real scoping gaps the package doesn't surface: the errorReporter-into-Home wiring and the catalogLoaded flag both require new prop-plumbing through files (main.tsx, App.tsx's loading state, AppShell.tsx) that aren't in the "files" list, so a builder following the list literally gets stuck partway through 2 of the package's 4 changes. Also surfaced a subtle correctness risk (duplicate errorReporter instance silently doubling the report-rate cap) and confirmed there's an established test pattern (App.test.tsx:4647) for exactly this kind of double-fire fix that the package should point at explicitly rather than leaving as an unstated expectation. None of this touches race timing, the run FSM, or any round-1-shipped decision — the core diagnosis is sound and most of the package (CSS truncation fixes, three double-submit locks, font-weight cleanup) is genuinely low-risk and ready to build as written. That's why this is amend, not drop; the file-list and test-requirement gaps are real enough that "keep" would leave a builder guessing on 2 of 4 changes.

## OWNER-PROXY RULING (binding)
RULING: ship, with Judge B's scope corrections. DROP the Home errorReporter.report sub-item (needs new plumbing with zero precedent — park it). The HUD hotfix has ALREADY shipped (d5a423e), so the .run-metrics tabular-nums change is unblocked — include it. Keep: path-history ellipsis fix (verify at 320/390 with a long title), board-row name ellipsis + right-aligned times, synchronous submit locks on continueAsGuest/createVGamesAccount/submitChallenge, tabular-nums on countdown + run-metrics, and the font-weight batch per the package text.
