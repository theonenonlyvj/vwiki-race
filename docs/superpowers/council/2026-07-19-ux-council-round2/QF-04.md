# QF-04 [P1/S] Color semantics: coral means commit, DNF stops being go-teal

## Files
- src/App.tsx:1450-1461 (End dialog buttons)
- src/App.tsx:1521 (VWiki kicker)
- src/components/BoardSnippet.tsx:48
- src/styles.css:1833-1836, 1980-1984, 383-392

## Changes
Give 'Confirm End Run' (and 'Confirm End Old Run') the coral treatment matching the HUD's .end-run-button; 'Continue run' stays neutral so safe-vs-commit is legible at a glance. In BoardSnippet, branch the rank span on row.rank === null to a .rank-dnf class colored #ffad9f (matching the leaderboard's existing DNF salmon) instead of unconditional var(--cyan). Retarget .challenge-list span from #ffad9f to var(--muted) so Browse cards stop burning coral on every row. Change .viota-mark to var(--text-bright), rename it vwiki-scoped, and fix App.tsx:1521 'VWiki' → 'VWiki Race'.

## Acceptance
- End dialog: the two buttons are visually distinct, coral only on the commit action
- DNF rows on Home/Results render salmon, never teal — no failed run shares the CTA color
- Browse cards contain zero coral; coral appears only on Start race / End Run commits
- Identity dialog kicker reads 'VWiki Race' in the standard bright text; no .viota-* class remains
- Re-capture the live flow through an actual Confirm End Run to the real Results screen (finish AND DNF, both breakpoints) — closes the PKG-05 evidence gap in the same pass

## Risk
Low — reuses existing tokens/classes; the DNF color matches an already-shipped convention one component over.

## Judge A (amend)
Widen the fix to the two other duplicate rank/DNF renderers, and tighten one branch condition, before shipping:

1. ADD src/modes/Boards.tsx:643-664 (Stats tab's inline DNF section) and src/components/LeaderboardList.tsx:90-111 (Challenge Detail's own Leaderboard panel DNF section) to the file list. Both render `<span className="rank">` inside a `.board-snippet` (same selector `.board-snippet .rank { color: var(--cyan) }`, styles.css:1833-1836), so both have the *identical* teal-DNF bug BoardSnippet.tsx has — confirmed live in the very screenshot this package's own evidence set contains (desktop-03-boards-today.png / mobile-03-boards-today.png: Stats/Today's DNF row renders its "–" rank in bright teal, matching the "Race" CTA, despite sitting in a `.muted`-wrapped section). As written, the package patches only 1 of these 3 sibling renderers and narrows its own acceptance criterion to "Home/Results" to make that pass — but "no failed run shares the CTA color" will still be false on Stats and Challenge Detail after this ships. Round 1's own README (docs/superpowers/council/2026-07-19-ux-council/README.md) already diagnosed "three leaderboard renderers" duplication as a root cause of drift; shipping a fix that touches only one of the three reproduces that exact failure mode rather than closing it. Still S/low-risk — same one-line class-branch, copy-pasted twice more.

2. FIX the branch condition itself: use `row.rankLabel === "DNF"` (or an explicit status check), not `row.rank === null`. `rank: null` is also produced by a legitimately-*completed*-but-unranked run (rankLabel "—", see src/domain/boardSnippet.ts:140-142 and dedupedRankForJustFinished:177-189 — containment-flagged/ranked_eligible=0 cases). Branching on `rank === null` would paint that row salmon too, visually relabeling a real completion as a failure and regressing the already-shipped invariant documented right there in the same file ("a completion is never demoted to DNF display," boardSnippet.ts:79).

3. ADD src/components/AdminDailies.tsx:168 (`<span className="viota-mark">VWiki</span>`) and its paired override at styles.css:2372 (`.daily-admin-heading .viota-mark { color: var(--coral); ... }`) to the file list. This is the package's own class's *second* call site — the stated acceptance criterion "no .viota-* class remains" is false as scoped, since this one is untouched.

Everything else checks out on direct verification: the End-dialog premise is real (App.tsx:1452/1459-1461 — both buttons are bare `<button>`, which defaults to `background: var(--cyan)` per styles.css:57-71, so today they're identically teal with zero danger cue on an irreversible action); the spec explicitly backs coral-for-commit ("coral reserved for primary/destructive race actions: Start, End Run," design spec line 256-257); the Browse "coral spam" is real and severe (every card in desktop-04-challenges.png/mobile-04-challenges.png renders its bare `<span>{challenge.label}</span>` at Browse.tsx:269 in #ffad9f via the unscoped `.challenge-list span` rule, confirmed unaffected by the existing `.browse-card-title-row`/`.state-chip` specificity overrides); and the proposed salmon convention for DNF ranks is precedented, not invented (`.leaderboard li.dnf .rank { color: #ffad9f }`, styles.css:2168-2170, already correctly applied in ChallengeDetail's "Your history" list). Keep effort S and risk Low — the additions above are copy-paste of an already-working pattern, not new design work.

### Judge A reasoning
Verified every cited file:line against the actual code and the round-2 screenshots; the package's core diagnosis is accurate and its fix direction is sound, evidenced, and spec-backed (design spec explicitly reserves coral for Start/End Run; the End dialog's two buttons are provably identical teal today; Browse's coral-spam claim is directly visible in desktop-04-challenges.png and traced to an unscoped `.challenge-list span` rule; the proposed DNF-salmon convention already exists correctly one component over in ChallengeDetail's "Your history" list).

However, the package under-scopes its own stated goal. There are actually four separate rank/DNF renderers in this codebase (BoardSnippet.tsx, Boards.tsx's inline section, LeaderboardList.tsx, and ChallengeDetail's "Your history" list), not one — and this is exactly the "three leaderboard renderers" duplication round 1's own README flagged as a systemic root cause of drift. The package fixes only BoardSnippet.tsx and then narrows its acceptance criterion to "Home/Results" to paper over the gap, but the teal-DNF bug is directly visible in the evidence set on Stats/Today (desktop-03-boards-today.png, mobile-03-boards-today.png), which is rendered by the untouched Boards.tsx, not BoardSnippet.tsx. Shipping as scoped would leave the most visible instance of the exact bug this package targets unfixed, while claiming victory — that's not consistent with an owner who wants "consistent, confident" and dislikes incomplete/re-litigated work. I also found the specific proposed branch condition (`row.rank === null`) would mis-color a legitimately-completed-but-unranked run as a DNF failure, regressing an invariant documented in the same file the package is editing — and a second `.viota-mark` call site (AdminDailies.tsx) that the package's own acceptance criterion implicitly requires but its file list omits.

All three corrections are cheap (copy the same one-line pattern to 2-3 more spots) and don't change the effort/risk classification — this stays a legitimate weekend quickfix, just needs its scope actually matched to its own acceptance claims before it ships.

## Judge B (amend)
1. SCOPE GAP — the rename can't hit its own acceptance bar as filed. `.viota-mark` has TWO live usages, not one: App.tsx:1521 (identity dialog, in scope) AND src/components/AdminDailies.tsx:168 (`<span className="viota-mark">VWiki</span>` in the daily-moderation heading), plus a second CSS rule at styles.css:2372 (`.daily-admin-heading .viota-mark { color: var(--coral); font-size: 0.68rem; }`) that targets it by name. Round-1's PKG-04 review explicitly noted both sites and called AdminDailies "correctly out of the package's scope" — deferred, not resolved. QF-04's "files" list only touches App.tsx:1521 and styles.css:383-392, but its acceptance line 3 says "no .viota-* class remains" unconditionally. As filed, that criterion is unmeetable. Add AdminDailies.tsx:168 and styles.css:2372 to files/changes; decide explicitly whether the admin-only heading keeps a coral mark (fine — it's not part of the user-facing commit-color grammar) under the renamed class, or also flips to text-bright.

2. CORRECTNESS RISK — `row.rank === null` is not a safe proxy for "is DNF" in BoardSnippet.tsx. `boardSnippetRowsForResult` (src/domain/boardSnippet.ts) also emits `rank: null` for a COMPLETED-but-unranked run (containment-flagged/excluded from the ranked CTE) — deliberately labeled "—", not "DNF", specifically to satisfy spec invariant 2 ("a completion is never demoted to DNF display", the exact thing PKG-03's rank-universe fix corrected last round). Branching the new `.rank-dnf` salmon color on `row.rank === null` would paint that "—" row the same danger-red as a genuine failure, quietly reintroducing the demotion the invariant forbids — just as a color bug instead of a label bug. Branch on the already-correct `row.rankLabel === "DNF"` (or thread an explicit status flag) instead of the nullable rank. Add a third re-capture fixture — completed-but-unranked — alongside the package's "finish AND DNF" acceptance line, since that's precisely the case a naive `rank === null` check gets wrong.

3. IMPLEMENTATION TRAP — reproduces the exact specificity-accident bug class round 1 just fixed twice. `.board-snippet .rank { color: var(--cyan); }` (styles.css:1833, specificity 0,2,0) directly targets the rank span (direct match beats inherited color regardless of the `.is-you` row highlight). A same-specificity `.rank-dnf` rule declared *before* it in source order — or written as a bare `.rank-dnf` (0,1,0) — will silently lose, exactly how `.challenge-list span` beat `.browse-card-title-row`/`.daily-badge` before PKG-04's fix (the README's own "systemic theme" #4). Spell out in "changes": the new rule must be `.board-snippet .rank-dnf` (or higher specificity) and declared after `.board-snippet .rank`.

4. CONSISTENCY GAP — "Continue run stays neutral" leaves it as a bare, unclassed `<button>`, which resolves to the same solid, saturated `var(--cyan)` fill as every other default button (styles.css:57-64) — not a receding/neutral look, just a second loud CTA next to the coral one. Round 1 (PKG-05, confirmed live at RaceResults.tsx:304) already shipped the precedent for this exact pairing: coral primary + `.secondary-button` (styles.css:345, `background: var(--ink-soft)`) for the alternate action, one screen over (Try again / View leaderboard). Add `className="secondary-button"` to "Continue run" so the End dialog matches the just-shipped sibling convention instead of drifting from it again — this is the more spec-consistent reading of "safe-vs-commit is legible at a glance" than leaving it default.

5. TEST GAP — package names zero tests despite touching two components with no existing coverage of this exact distinction: there is no BoardSnippet.test.tsx (only domain/boardSnippet.test.ts, which tests data shaping, not rendering/classNames), and App.test.tsx's only `.end-run-button` assertion (line 996) is against the HUD's trigger button, not the dialog's confirm button. Round 1's owner-proxy ruling for the structurally identical PKG-04 coral/teal split explicitly required "RTL class-assertion guards." Name at minimum: (a) an App.test.tsx assertion that "Confirm End Run"/"Confirm End Old Run" carries `end-run-button` and "Continue run" carries `secondary-button` (or lacks `end-run-button`), and (b) a BoardSnippet render test asserting `.rank-dnf` applies only to true DNF rows, not placement rows or completed-unranked ("—") rows.

None of the above changes the package's shape or effort tier — all five are additions/precisions within the same S-effort, two-component-plus-CSS footprint, no touch to race timing, run lifecycle, or App.tsx's exit state machine.

### Judge B reasoning
Verified every code citation against the live tree. App.tsx:1441-1463 confirms the End dialog: both "Continue run" and "Confirm End Run"/"Confirm End Old Run" are today bare `<button>`s with no className, which resolve to the global default rule (styles.css:57-64, solid `var(--cyan)` fill) — so today there genuinely is zero visual distinction between the two, confirming the "no danger cue on irreversible choice" premise. `.end-run-button` (styles.css:515-524) exists and is reserved for exactly this semantic (used by RaceMode's HUD trigger, styles.css:1229) per round-1's PKG-04 owner-proxy ruling ("coral is ONLY for clock-commit and destructive race actions... 'End Run'") and the design spec (docs/.../2026-07-18-ux-redesign-modes-design.md:255-258, "coral reserved for primary/destructive race actions: Start, End Run" — RESOLVED). So the core color move is squarely spec-compliant and extends an already-ratified convention to a screen it hadn't reached yet — not a re-litigation of anything that shipped. BoardSnippet.tsx:48 and styles.css:1833/1980/383 line citations are all accurate. The `.challenge-list span` retarget is a real, previously-unaddressed bug: the bare `<span>{challenge.label}</span>` inside `.challenge-meta` (Browse.tsx:269) still matches `.challenge-list span`'s (0,1,1) coral rule because `.challenge-list .challenge-meta`'s `color:inherit` override (0,2,0) targets the meta wrapper, not this nested bare span — PKG-04 fixed the title/meta/chip lines but missed this one, so QF-04's retarget is a genuine additional fix, not redundant.

Found two classes of problems that push this to amend rather than keep: (a) a real correctness/invariant risk (BoardSnippet's `rank===null` branch would re-introduce a completion-demoted-to-DNF-display bug in the "—" case, and a same-specificity CSS ordering trap that mirrors a bug class round 1 spent two packages fixing), both concrete and reachable via code paths I traced (boardSnippetRowsForResult in src/domain/boardSnippet.ts), not speculative; and (b) a scope-completeness gap (AdminDailies.tsx's second `.viota-mark` site, confirmed by grep and by round-1's own PKG-04 review noting it as deferred) that makes the package's own acceptance criterion literally unsatisfiable as filed. Also flagged a missed opportunity to reuse the already-shipped `.secondary-button` convention (confirmed live at RaceResults.tsx:304) for full internal consistency, and a test-coverage gap consistent with round 1's own precedent of requiring RTL guards for identical coral/teal semantic splits. None of these require redesign or owner input — all are precise, low-effort additions inside the same files/effort tier — so amend, not drop; and not a clean keep because the plan as written would ship a real regression (DNF-red on a legitimately-completed run) and fail its own stated acceptance criterion.

## OWNER-PROXY RULING (binding)
RULING: ship WIDENED per Judge A — fix the teal-DNF rank bug in ALL THREE renderers (BoardSnippet, Boards inline DNF section, LeaderboardList) with one shared class; tighten the branch condition per his note. Per Judge B: the .viota-mark rename must cover BOTH usages (App.tsx identity dialog AND AdminDailies.tsx heading) plus the styles.css:2372 rule, or the acceptance bar fails.
