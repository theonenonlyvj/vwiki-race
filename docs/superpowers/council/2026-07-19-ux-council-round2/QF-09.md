# QF-09 [P2/M] You tab: one empty state, two missing averages, un-collide 'Stats'

## Files
- src/modes/You.tsx:28-123

## Changes
When the account has zero attempts, collapse the 7-tile grid + 3 list sections into one warm empty state ('Play your first race to start building stats') with a CTA back to Home — never repeat the same placeholder string 10 times. For accounts with history, add 'Avg speed' and 'Avg clicks' tiles from totals.averageElapsedMs/averageClicks (already typed, validated, and delivered on every response — currently discarded at render). Rename You's <h2>Stats</h2> to 'Your stats' so the nav tab literally labeled Stats stops pointing at a different screen than the heading.

## Acceptance
- Fresh guest You tab shows a single message + CTA; zero repeated 'No data yet.' strings
- Played account sees the grid including the two average tiles, formatted with the existing helpers
- No screen's content heading duplicates a different tab's nav label

## Risk
Low — single file; the consolidation pattern (Boards-style progress framing) was already ordered by PKG-11.

## Judge A (amend)
1. Fix the file scope: the 'CTA back to Home' requires AppShell.tsx too, not just You.tsx:28-123. AppShell.tsx already has the exact precedent one line away — Browse gets `onGoHome={() => onSelectMode("home")}` at AppShell.tsx:281. Add the same one-line `onGoHome` prop to the `<You .../>` call at AppShell.tsx:293-297 and a matching `onGoHome: () => void` prop on You's signature. Trivial and precedented, so this doesn't change the effort/risk rating — just correct the file list so the diff isn't a surprise.

2. Make the collapse trigger explicit and cover both cases the finding conflates under 'zero attempts': (a) unclaimed/guest — `stats` prop is null (App.tsx:323-325 short-circuits to null with no identitySession), and (b) a claimed account that has resolved with a genuine 0-attempt totals object. Right now PKG-11's shipped behavior (You.tsx:75-123, comment at 62-74) deliberately shows real '0' values in the 7-tile grid for case (b) rather than collapsing it — that was Judge A's ratified amendment 3 option (b) from round 1. QF-09's one-empty-state design supersedes that for the zero-attempts case specifically, which is fine and is a reasonable improvement now that the 'own ticket' PKG-11 punted has arrived — but say so explicitly in the package text ('this revises PKG-11's zero-totals tile display for the specific case of confirmed-zero attempts; non-zero totals with individually-null bestClicks/bestElapsedMs keep showing "No data yet." per PKG-11, unchanged') so the diff doesn't read as an unexplained reversal of a binding round-1 ruling to whoever reviews it next.

3. Call out one edge case so it isn't later filed as a regression: `accountStatsProjection` resets `stats` to null on every statsRefreshVersion bump (App.tsx:559-581), which fires after every run-ending event — so a returning player who finishes a race and immediately taps 'You' can see a sub-second flash of the new 'Play your first race' empty state before their real grid loads, using the same stats===null signal as the true guest case. This is the same loading/error/guest conflation Judge A already flagged in PKG-11 (round 1) and the team knowingly left unsolved because untangling it needs new state plumbing across App.tsx → AppShell.tsx → You.tsx. Don't scope that plumbing into this package (it would blow past M/low-risk) — just note it as a known, acceptable, transient trade-off in the brief so it isn't surprised into a future 'critical regression' finding.

4. Minor: confirm the CTA copy ('Play your first race to start building stats') reads correctly for the claimed-zero-attempts case too, not only the true guest case — a logged-in player with a real account name seeing this message should feel the same as a guest seeing it; no separate copy branch needed, just confirm during implementation.

### Judge A reasoning
Verified every load-bearing claim directly against code and the cited screenshot. mobile-07-you.png confirms exactly ten repeated 'No data yet.' strings (7 grid tiles + 3 list sections) for the guest state — the package's headline framing is accurate, not exaggerated. AccountStats.totals.averageElapsedMs/averageClicks are real, typed (domain/types.ts:170-171/214-215), server-computed (d1TrackingRepository.ts:2858-2859) and unit-tested fields that You.tsx's StatsPanel (lines 77-123) never reads — genuinely dead-but-delivered data, exactly as claimed. The naming collision is real and was not caught in round 1: PKG-14 (direct, binding owner feedback) renamed the Boards/leaderboards nav tab and its own `<h2>` to the literal string 'Stats' (AppShell.tsx:28, Boards.tsx:402-403) per "Boards - rename to stats", while You.tsx's own heading (line 82) already said 'Stats' for a different screen one tap away — nobody caught that these two bottom-nav tabs now both self-identify as 'Stats.' Renaming You's heading to 'Your stats' doesn't touch or contradict the owner's explicit Boards rename, so there's no conflict with a binding ruling.

Critically, the empty-state consolidation ask is NOT round-1 re-litigation despite superficially resembling one: PKG-11's own text (council README + PKG-11.md Judge A amendment 3) explicitly separated 'consolidate the nine stacked blanks into the Boards-style progress framing' out as a distinct, larger empty-state-layout decision, and the team's shipped resolution (in-code comment at You.tsx:62-74, tagged 'Judge A amendment 3, option b') explicitly punted it — 'descoped to its own ticket per the council rescope.' QF-09 is that follow-up ticket. The brief's instruction not to re-litigate decisions that 'shipped as designed' doesn't apply to a decision whose own text says 'defer this to a follow-up' — this is finishing deferred work, not reopening settled work.

The package is real, well-evidenced, and taste-positive: it turns the app's most embarrassing screen (ten copies of a placeholder string) into one warm message, surfaces two already-computed stats a returning player in a stats-and-race game would plainly want (avg speed/clicks), and removes a genuine two-taps-apart naming collision — all without touching anything round 1 ratified as intentional (teal CTAs, race framing, Boards' 'Stats' name itself). Effort M and 'low risk' hold up with minor caveats: the CTA wiring touches one extra file (AppShell.tsx) via an already-established one-line pattern used by Browse, and the zero-vs-loading conflation the fix reuses is a known, low-severity, self-correcting edge case rather than a new bug — worth naming explicitly in the brief so it doesn't get rediscovered later as a false-alarm regression, but not a reason to reject or resize the package. Verdict: amend, not drop — the core is sound and worth shipping, the amendments are scope/documentation tightening, not redesign.

## Judge B (amend)
1) Replace 'When the account has zero attempts, collapse...' with an explicit, unambiguous branch condition. Recommend scoping the single-message empty state to the case the screenshot evidence actually shows — `stats === null` — and choosing one of: (a) restrict further to `identitySession === null` (true guest, unambiguous 'never even tried'), leaving the loading/error sub-cases exactly as today (still-repeating placeholders, but at least not newly wrong) and filing loading-vs-error disambiguation as its own ticket per the PKG-11 owner ruling; or (b) if collapsing on bare `stats === null` regardless of cause, explicitly accept and note in the package that a transient fetch error for an already-played identified account will show 'Play your first race to start building stats' — a known false-negative, not a silent gap. Don't leave 'zero attempts' as the literal implementable spec; it doesn't match the null-stats case that produces the cited 10x bug.
2) Add `src/App.test.tsx` to `files`, naming the exact assertions that break on the h2 rename: line 3007 (nav-cycle test's post-You-click Stats-heading check), line 3061 ('shows the account's stats in You'), and line 4491 (PKG-11's 'You shows a streak tile...' test). Add an acceptance criterion requiring a new test for the fresh-guest single-message+CTA path, since none exists today.
3) Specify the `averageClicks` display: no formatter exists for it anywhere in the codebase (confirmed via grep — it's currently unrendered). Pick a precision (e.g. `.toFixed(1)`, consistent with Boards.tsx:479's `avgPlacement` precedent) and state it in `changes` rather than implying an existing helper covers both new tiles.

### Judge B reasoning
Core claims verified true and the direction is sound, but the package as written has an implementable trigger-condition bug plus an incomplete files/test list.

Confirmed accurate: (1) AppShell.tsx:28 sets the boards nav label to literally "Stats" (PKG-14 rename), and You.tsx:82 independently renders `<h2>Stats</h2>` — a genuine, verified nav/heading collision, not a nitpick. (2) `averageClicks`/`averageElapsedMs` are typed on `AccountStats.totals` (domain/types.ts:214-215), computed server-side (domain/stats.ts:54-55, server/d1TrackingRepository.ts:2858-2859), validated on ingest (services/vwikiRaceApiClient.ts:768-769), and covered by tests — but grep confirms zero render sites anywhere in src/. Genuinely wired-and-discarded, as claimed. (3) The 10x-repeat evidence is real: with `stats === null` (App.tsx:323-326, true for guest/loading/error alike), You.tsx's 7-tile grid + 3 StatsList sections all fall through to `NO_DATA_YET`, producing exactly 10 instances — matches the cold-critic citation.

Three problems an implementer needs resolved before this ships safely:

1. TRIGGER-CONDITION MISMATCH (real risk). The package's own prose says 'When the account has zero attempts, collapse...' — read literally that's `totals.attempts === 0`, which requires a non-null `totals`. But the cited 10x-repeat bug fires on `stats === null` (App.tsx:323-326 conflates guest / still-loading / fetch-error into that one null), which has no `totals` at all. This is the exact ambiguity PKG-11's own council already surfaced and explicitly punted ('stats===null today conflates loading, errored, and no-session — no existing signal distinguishes them', PKG-11.md Judge B reasoning) with an owner-proxy ruling to keep it out of scope. QF-09 needs to state its actual branch condition explicitly and — critically — decide what a transient stats-fetch error shows for an already-played, identified account: collapsing on bare `stats===null` would tell a returning player 'Play your first race to start building stats,' which is actively false, not just a missing placeholder. At minimum, scope the collapse to the unambiguous case (`identitySession === null`, i.e. true guest who never attempted an account) or explicitly accept/flag the error-state tradeoff — don't leave the branch condition to an implementer's literal reading of 'zero attempts.'

2. MISSING TEST-UPDATE REQUIREMENT (concrete, enumerable — verified in App.test.tsx). Renaming the You `<h2>` from 'Stats' to 'Your stats' will break at least three existing assertions that check `screen.getByRole("heading", { name: "Stats" })` while on the You tab: the nav-cycling test at App.test.tsx:3007 ('...You...aria-pressed...true' followed by the Stats-heading check), 'shows the account's stats in You' at App.test.tsx:3061, and PKG-11's own test 'You shows a streak tile reusing accountStats.dailyStreak' at App.test.tsx:4491. The package's `files` list is only `src/modes/You.tsx:28-123` — add `src/App.test.tsx` with these three line numbers, plus a new test for the fresh-guest single-message+CTA state (no existing coverage of that path today, so 'acceptance' criterion 1 is currently unverifiable by any test in the suite).

3. 'Formatted with the existing helpers' overstates what exists. `formatElapsed` (race/shared.tsx:14-16) covers `averageElapsedMs` fine, but grep confirms no formatter for `averageClicks` exists anywhere in the codebase today (it's never rendered). This needs a small explicit decision (e.g. `.toFixed(1)`, matching the one precedent for a fractional per-run stat, Boards.tsx:479's `avgPlacement.toFixed(1)`) rather than being treated as pure wiring.

None of this touches spec invariants (time+clicks, completion precedence, no-run-until-Start, identity-at-Start-only, paths-hidden, casual framing) or reopens any shipped round-1 package — it's confined to You.tsx render logic plus test assertions, so blast radius stays low and 'M' effort is still roughly right once the amendments are folded in. Amend, don't drop: the collision and the discarded-fields findings are both solid, well-evidenced P2s worth shipping this cycle.</reasoning>
</invoke>


## OWNER-PROXY RULING (binding)
RULING: ship with both judges' scoping: empty-state branch = stats === null AND identitySession === null (true never-played guest); loading/error sub-cases untouched this pass. Add the onGoHome prop via the exact AppShell precedent Judge A cited. Add the two average tiles from already-delivered totals fields. Rename the You heading to 'Your stats'.
